using UnityEngine;

public class MazeBool : MonoBehaviour
{
public bool mazeBool = false;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
